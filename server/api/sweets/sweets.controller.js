'use strict';

var Sweets  = require('./sweets.model');


/**
 * Get list of users
 * restriction: 'admin'
 */
exports.all = function(req, res) {
  return res.status(200).json({
    aac: "asd"
  });
};
